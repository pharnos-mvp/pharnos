# Cloudflare Pages — réécritures de la landing pharnos.com.
# Lien d'invitation nominatif d'un expert : /i/CODE → page unique (le JS lit le code dans l'URL).
# ⚠️ Cible SANS extension : « /invite.html » déclenche le 308 « pretty URLs » de Pages vers
# /invite, qui PERD le code du chemin — « /invite » est une réécriture interne, chemin préservé.
/i/* /invite 200

# Le CTD Builder ne vit plus sous un chemin de ce site : il a son propre domaine,
# builder.pharnos.com (projet Pages `pharnos-builder`). C est ce qui a rendu son repli SPA
# possible — a la racine d un projet, `/* /index.html 200` fonctionne ; sous un chemin d ici,
# les deux formes essayees en production le 2026-08-03 ont echoue (l une ne s appliquait
# jamais, l autre capturait les assets et blanchissait la page). Ne pas re-introduire de regle
# `/ctdbuilder*` : `ctdbuilder.html` est une PAGE de ce site, servie normalement.
